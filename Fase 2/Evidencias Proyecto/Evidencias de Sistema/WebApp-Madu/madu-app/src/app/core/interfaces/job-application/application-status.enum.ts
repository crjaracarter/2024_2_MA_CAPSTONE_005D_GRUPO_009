// src/app/core/interfaces/job-application/application-status.enum.ts
export enum ApplicationStatus {
    PENDING = 'pending',
    IN_REVIEW = 'in_review',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected'
  }